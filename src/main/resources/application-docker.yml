micronaut:
  server:
    port: 8080

coop:
  stlma:
    tech:
      protocolsn:
        plugin-name: ProtocolSNUserPlugin
        plugin-host: psn-user-plugin
        plugin-grpc-port: 50051

grpc:
  server:
    port: 50051
  channels:
    node-manager:
      target: psn-node-manager:50051
      plaintext: true
